numpy>=1.26
PyYAML>=6.0
pyzmq>=25
opencv-python>=4.8
# Intel RealSense 全景彩色相机（也可使用 librealsense 自带的安装包）：
# pyrealsense2>=2.55
# 设备 A 可视化（仅 --visualize 时需要）：
# pyqtgraph>=0.13
# PyOpenGL>=3.1
# PySide6>=6.5
# LeRobot 0.4 可写 Dataset v3，停止 episode 时由 PNG 编码最终 MP4：
# lerobot>=0.4,<0.5
# 若 Python/硬件 SDK 环境允许，推荐使用支持 streaming_encoding/resume 的新版：
# lerobot>=0.6,<0.7
# 设备 A 真机：pyzmq、pysurvive/libsurvive
# 设备 B 真机：pyAgxArm、aero_open_sdk、opencv-python
