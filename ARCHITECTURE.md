# 架构与验证约束

## 时间

进程内只使用 `time.perf_counter_ns()`，禁止用墙上时间参与对齐。A 包含源单调时间；
B 使用时钟映射后写入缓冲。帧时刻由 B 的绝对 deadline 驱动，并采用 causal
sample-and-hold，避免选到“未来”样本。数据集中保存每种模态相对帧时刻的 lag 和
valid。系统每 1 秒执行一次已接入 UDP 通道的四时间戳同步握手，`ClockMapper`
选择低 RTT 样本的偏移中位数。

## 安全状态机

建议左右侧分别实现 `DISCONNECTED → READY → ARMED → TELEOP → FAULT/ESTOP`，
并在整机层增加双侧联锁急停。
只有 ARMED 且收到独立使能信号时才进入 TELEOP。以下条件保持最后安全位姿并停止：
遥操作超时、IK 失败、设备掉线、非有限值、急停。每周期依次执行工作空间、关节限位、
速度/加速度限幅和碰撞检查。Demo 已实现超时、工作空间、步进限幅和手关节限位。

## 背压

所有跨线程通道有界。时间缓冲丢最旧样本；写入队列满时丢最旧 LeRobot frame。
任何情况下 writer 不得持有机器人适配器的锁。预览从相机缓冲只读抽样，不能插入
采集或控制路径。

## 验收

1. 断开 A 网络，两侧机器人都在 `teleop_timeout_ms` 内保持/停止。
2. 人为给 writer 加 200 ms 延时，控制频率仍达到目标且仅增加 `writer_drops`。
3. 注入乱序/重复包，B 不执行旧序列。
4. 分别遮挡左腕、右腕和全景相机的计时器画面，检查各路 `alignment.lag_s`。
5. 对输出运行 LeRobot dataset 加载、首尾帧解码、shape/dtype/NaN 校验。
6. 急停、CAN/串口断线、相机掉线分别注入，日志必须包含设备、seq、单调时间和原因。
